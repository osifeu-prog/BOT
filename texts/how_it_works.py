"""
texts/how_it_works.py
======================
מכיל את ההסבר "מפת־על: איך הבוט עובד".

כאן אתה מדביק את הטקסט הארוך שהגדרת:
- /start → router → start_handler
- callback → callback_router → menu_callback
- /admin → admin_handler
- "אושר" → send_zip
- רישום ב-DB וכו'.
"""

HOW_IT_WORKS = """
🧠 מפת־על: איך הבוט שלך עובד — מההתחלה ועד הסוף

(כאן תדביק את כל הטקסט המלא שכתבת)
"""
HOW_IT_WORKS = """
🧠 מפת־על: איך הבוט עובד — מההתחלה ועד הסוף

────────────────────────────────────────

🔵 1. משתמש שולח /start

מה קורה בפועל:

• טלגרם שולח את ההודעה ל־Webhook שלך  
• FastAPI מקבל אותה ב־/webhook  
• main.py מזהה שיש "message"  
• הוא שולח את זה ל־handle_message()  
• router.py בודק:
    – האם המשתמש מנהל?
    – האם זו פקודת /admin?
    – האם זו פקודת /start?

אם זו /start → start_handler() מופעל

מה עושה start_handler:

• שולח תמונה (מה־URL שלך)  
• שולח הודעת ברוך הבא  
• מציג תפריט כפתורים  

────────────────────────────────────────

🔵 2. משתמש לוחץ על כפתור בתפריט

מה קורה בפועל:

• טלגרם שולח "callback_query"  
• FastAPI מזהה זאת  
• main.py שולח ל־handle_callback()  
• callback_router.py בודק:
    – האם המשתמש מנהל?
    – האם ה־callback מתחיל ב־menu_?

אם כן → menu_callback() מופעל

מה עושה menu_callback:

• רושם את האירוע ב־DB  
• אם זה "menu_buy" → שולח הודעת תשלום  
• אחרת → שולח הודעת “נבחר: X”  

────────────────────────────────────────

🔵 3. משתמש רוצה להפוך למנהל

הוא שולח:

/admin <password>

מה קורה בפועל:

• router.py מזהה /admin  
• מפעיל admin_handler()  
• admin_handler בודק:
    – אם זה אתה (ADMIN_ID) → תמיד מאושר  
    – אם הסיסמה נכונה → מוסיף לטבלת admins  
    – אם לא → שולח “סיסמה שגויה”

מאותו רגע:

• המשתמש מנהל מלא  
• יכול להשתמש בבוט  
• יכול ללחוץ כפתורים  
• יכול לקבל ZIP  
• יכול לבצע פעולות נוספות  

────────────────────────────────────────

🔵 4. משתמש רוצה לקנות את הפרויקט

הוא לוחץ:

"רכישת הפרויקט"

מה קורה בפועל:

• callback מגיע  
• menu_callback() מופעל  
• הוא מזהה menu_buy  
• שולח הודעת תשלום:

עלות: X ש"ח  
תשלום ל־TON_WALLET  
שלח צילום מסך לאחר התשלום  

────────────────────────────────────────

🔵 5. אתה מאשר תשלום

המשתמש שולח:

"אושר"

מה קורה בפועל:

• router.py מזהה שהטקסט מתחיל ב־"אושר"  
• מפעיל send_zip()  
• הבוט שולח:

📦 הנה הקובץ שלך:
<ZIP_LINK>

────────────────────────────────────────

🔵 6. כל פעולה נרשמת ב־DB

כל הודעה / כפתור / פקודה נרשמים בטבלה:

user_events

כל מנהל נרשם בטבלה:

admins

זה נותן לך:

• מעקב  
• סטטיסטיקות  
• יכולת ניתוח  
• יכולת חסימה  
• יכולת ניהול משתמשים  

────────────────────────────────────────

🧩 תרשים זרימה טקסטואלי (ברמת מוצר)

/start → router → start_handler → תמונה + תפריט

כפתור → callback_router → menu_callback  
    ├── menu_buy → הודעת תשלום  
    └── menu_help → הודעת עזרה  

/admin <password> → admin_handler  
    ├── סיסמה נכונה → add_admin  
    └── סיסמה שגויה → הודעת שגיאה  

"אושר" → send_zip → שולח ZIP  

כל פעולה → events.log → DB  

────────────────────────────────────────

🟦 הצעות לשדרוגים חכמים (שיהפכו את הבוט למוצר אמיתי)

⭐ 1. מערכת רישיונות (License Keys)  
• כל משתמש מקבל קוד חד־פעמי  
• ניתן לייצר קודים מראש  
• או ליצור קוד אוטומטי אחרי תשלום  

⭐ 2. Dashboard למנהלים  
• ממשק וובי קטן  
• כמה משתמשים  
• כמה רכישות  
• כמה מנהלים  
• כמה אירועים  
• גרפים  

⭐ 3. מערכת אימות תשלום אוטומטית  
• חיבור ל־TON API  
• או Telegram Payments  
• או CryptoPay  
• אין צורך בצילום מסך  

⭐ 4. מערכת הודעות Broadcast  
• שליחת הודעה לכל המנהלים / לכל המשתמשים  

⭐ 5. מערכת קבצים מתקדמת  
• במקום ZIP אחד → ספריית קבצים עם הרשאות  

⭐ 6. מערכת תמיכה (Support)  
• כפתור “תמיכה” שמחבר אותך ישירות למשתמש  

────────────────────────────────────────

רוצה שאכין לך תרשים זרימה גרפי נוסף או שדרוגים נוספים?
"""
