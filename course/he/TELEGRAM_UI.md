# 🎛 ממשק טלגרם – כל האפשרויות

בשיעור הזה נלמד על סוגי הממשקים שבוט יכול להשתמש בהם בטלגרם, ואיך זה מתחבר לקוד שלך.

--- PAGE ---

## Reply Keyboard

Reply Keyboard היא מקלדת שמופיעה במקום המקלדת הרגילה.

מאפיינים:

- נשארת פתוחה עד שמחליפים אותה
- מתאימה לזרימות פשוטות
- פחות מדויקת לניתוח (אין callback_data)

בקורס הזה אנחנו מתמקדים יותר ב־Inline Keyboard, אבל חשוב להכיר.

--- PAGE ---

## Inline Keyboard

Inline Keyboard היא מקלדת שמופיעה מתחת להודעה.

מאפיינים:

- כל כפתור שולח `callback_query`
- יש `callback_data` – טקסט קצר שמגיע לבוט
- מאפשרת לוגיקה מורכבת (תפריטים, קורסים, משחקים)

בקוד שלנו:

- `buttons/menus.py` בונה את ה־Inline Keyboard
- `handlers/callback_router.py` מנתב לפי `callback_data`

--- PAGE ---

## Bot Commands

פקודות כמו `/start`, `/admin`, `/help`.

- אפשר להגדיר אותן ב־BotFather
- הן מופיעות למשתמש בתפריט הפקודות
- בקוד, אנחנו בודקים `text.startswith("/start")` וכו'.

פקודות הן נקודת כניסה טובה לזרימות שונות.

--- PAGE ---

## Bot Menu

בטלגרם אפשר להגדיר "Bot Menu" קבוע:

- רשימת כפתורים שמופיעה למשתמש
- אפשר להגדיר דרך BotFather
- לדוגמה: "Start", "Help", "Buy"

בקורס הזה אנחנו לא מגדירים אותו בקוד, אבל אפשר להוסיף.

--- PAGE ---

## WebApps

WebApp מאפשר לפתוח חלון Web בתוך טלגרם:

- לדוגמה: טופס מורכב, דשבורד, חנות
- דורש Frontend (HTML/JS)
- הבוט מקבל נתונים מה-WebApp

זה שלב מתקדם יותר – אפשר להוסיף כמודול עתידי.

--- PAGE ---

## Attachment Menu

בוט יכול להופיע בתפריט ה"ניירת" (Attachment):

- מאפשר למשתמש לבחור את הבוט מתוך תפריט שיתוף
- מתאים לבוטים שעובדים עם קבצים / תמונות

זה פחות רלוונטי לקורס הזה, אבל טוב לדעת שזה קיים.

--- PAGE ---

## איך זה מתחבר לקורס שלנו?

בקורס הזה:

- אנחנו משתמשים בעיקר ב־Inline Keyboard
- כל כפתור מייצג:
  - תפריט
  - שיעור
  - עמוד בקורס
  - משחק

ההבנה של Telegram UI עוזרת לך לעצב חוויית משתמש טובה יותר.
