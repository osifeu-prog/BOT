
# Telegram Product Bot (Production Ready)

## Run locally
```bash
pip install -r requirements.txt
python main.py
```

## Environment Variables
All variables are injected from the hosting platform (Railway/Render/etc).
No hardcoded secrets.
