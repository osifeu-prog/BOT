# 🎰 NFTY ULTRA PRO - Requirements
# גרסה מלאה עם כל החבילות המתקדמות לאנימציות, UI, וביצועים גבוהים

# ============ CORE TELEGRAM ============
python-telegram-bot==21.7  # גרסה חדשה עם תמיכה מלאה
python-telegram-bot[job-queue]==21.7  # תורים לפעולות מתוזמנות

# ============ DATABASE & CACHE ============
redis==5.0.1  # Redis client
redis-py-cluster==2.1.3  # תמיכה ב-cluster
hiredis==2.2.3  # ביצועים גבוהים ל-Redis
fakeredis==2.20.0  # לבדיקות

# ============ WEB FRAMEWORK & ASYNC ============
fastapi==0.104.1  # Web framework מודרני
uvicorn[standard]==0.24.0  # שרת ASGI מהיר
httpx==0.25.1  # HTTP client אסינכרוני
aiohttp==3.9.1  # עוד HTTP client אסינכרוני
asyncio-throttle==1.0.2  # הגבלת תעבורה

# ============ DATA PROCESSING ============
pandas==2.2.0  # עיבוד נתונים מתקדם
numpy==1.26.3  # חישובים מתמטיים
polars==0.20.1  # DataFrame מהיר במיוחד
openpyxl==3.1.2  # קריאת/כתיבת Excel
python-multipart==0.0.6  # טיפול בקבצים

# ============ VISUALIZATION & ANIMATIONS ============
matplotlib==3.8.2  # גרפים מתקדמים עם אנימציות
seaborn==0.13.0  # גרפים סטטיסטיים יפים
plotly==5.18.0  # גרפים אינטראקטיביים
chartjs==0.1.2  # אינטגרציה עם Chart.js
Pillow==10.2.0  # עיבוד תמונות
imageio==2.33.0  # יצירת GIFs ואנימציות
imageio-ffmpeg==0.4.9  # תמיכה בווידאו
emoji==2.10.1  # אמוג'ים בתצוגה

# ============ UI/UX ENHANCEMENTS ============
rich==13.7.0  # UI עשיר בטרמינל
termcolor==2.4.0  # צבעים בקונסולה
colorama==0.4.6  # צבעים חוצי פלטפורמות
tabulate==0.9.0  # טבלאות יפות
progress==1.6  # מונה התקדמות
tqdm==4.66.1  # progress bars
alive-progress==3.1.5  # progress bars אנימציה

# ============ PAYMENTS & CRYPTO ============
cryptography==41.0.7  # הצפנה
pycryptodome==3.20.0  # כלים קריפטוגרפיים
blockchain==1.4.4  # אינטגרציה עם בלוקצ'יין
coinbase==2.1.0  # Coinbase API
web3==6.11.3  # אינטראקציה עם Ethereum
ton-client-py==1.37.0  # TON Blockchain
aiogram==3.3.0  # חלופה ל-telegram bot

# ============ SECURITY & VALIDATION ============
python-jose[cryptography]==3.3.0  # JWT tokens
passlib==1.7.4  # hashing passwords
bcrypt==4.1.2  # hashing secure
argon2-cffi==23.1.0  # hashing חזק
pyjwt==2.8.0  # JSON Web Tokens
pydantic==2.5.1  # ולידציית נתונים
pydantic-settings==2.1.0  # הגדרות עם ולידציה
email-validator==2.1.0  # ולידציית אימיילים

# ============ PERFORMANCE & OPTIMIZATION ============
ujson==5.8.0  # JSON מהיר מאוד
orjson==3.9.10  # JSON מהיר ביותר
msgpack==1.0.7  # סיריאליזציה בינארית
lz4==4.3.2  # דחיסה מהירה
zstandard==0.22.0  # דחיסה מתקדמת
pysimdjson==5.0.2  # JSON מהיר במיוחד

# ============ MONITORING & ANALYTICS ============
prometheus-client==0.19.0  # מדדים
statsd==4.0.1  # סטטיסטיקות
datadog==0.48.0  # מוניטורינג
sentry-sdk==1.40.3  # טיפול בשגיאות
loguru==0.7.2  # לוגינג מתקדם
structlog==23.2.0  # structured logging

# ============ UTILITIES ============
python-dotenv==1.0.0  # ניהול environment variables
requests==2.31.0  # HTTP requests
beautifulsoup4==4.12.2  # web scraping
lxml==4.9.3  # XML/HTML processing
pyyaml==6.0.1  # YAML parsing
toml==0.10.2  # TOML parsing
jsonschema==4.20.0  # ולידציית JSON
typeguard==4.2.1  # בדיקת טיפוסים
mypy==1.8.0  # static type checking

# ============ AI & ML ENHANCEMENTS ============
openai==1.6.1  # ChatGPT API
langchain==0.1.0  # AI chains
chromadb==0.4.22  # וקטור דאטאבייס
sentence-transformers==2.2.2  # embeddings
nltk==3.8.1  # עיבוד שפה טבעית
textblob==0.18.0  # NLP פשוט
transformers==4.36.2  # מודלי AI

# ============ GAME SPECIFIC ============
pygame==2.5.1  # אנימציות משחקים מתקדמות
pymunk==6.5.0  # פיזיקה למשחקים
noise==1.2.2  # יצירת noise procedural
perlin-noise==1.12  # Perlin noise

# ============ DATE & TIME ============
pytz==2023.3.post1  # timezones
dateutils==0.6.12  # כלי תאריכים
arrow==1.3.0  מניפולציית תאריכים
pendulum==2.1.2  תאריכים מתקדמים

# ============ TESTING ============
pytest==7.4.3  # בדיקות
pytest-asyncio==0.21.1  # בדיקות אסינכרוניות
pytest-cov==4.1.0  # כיסוי קוד
pytest-mock==3.12.0  # mocking
hypothesis==6.92.5  # property testing
factory-boy==3.3.0  # יצירת נתוני בדיקה
freezegun==1.2.2  # הקפאת זמן לבדיקות

# ============ DEVELOPMENT TOOLS ============
black==23.12.1  # code formatting
isort==5.13.2  # סידור imports
flake8==6.1.0  # בדיקת קוד
mypy==1.8.0  # טיפוסים סטטיים
pre-commit==3.6.0  # hooks לפני commit
bandit==1.7.6  # אבטחת קוד
safety==2.3.5  # בדיקת חבילות

# ============ DEPLOYMENT ============
gunicorn==21.2.0  # WSGI server
daphne==4.0.0  # ASGI server
supervisor==4.2.5  # process management
circus==0.18.0  # process manager
honcho==1.1.1  # process manager לפיתוח

# ============ AUDIO/VISUAL EFFECTS ============
simpleaudio==1.0.4  # השמעת סאונד
pydub==0.25.1  # עיבוד אודיו
wave==0.0.2  # טיפול בקבצי WAV
soundfile==0.12.1  # קריאת קבצי סאונד

# ============ CUSTOM ============
retrying==1.3.4  # ניסיונות חוזרים
backoff==2.2.1  # exponential backoff
tenacity==8.2.3  # retry עם תצורות מתקדמות
cachetools==5.3.2  # caching מתקדם
ratelimiter==1.2.0.post0  # הגבלת תעבורה

# ============ WEBHOOKS & WEB ============
websockets==12.0  # WebSockets
aiofiles==23.2.1  # קבצים אסינכרוניים
jinja2==3.1.2  # טמפלייטים
markdown==3.5.1  # סימון Markdown
markdown2==2.4.10  # Markdown מהיר
