# 🏗 ארכיטקטורת המערכת

בשיעור הזה נבין איך כל התיקיות והקבצים במערכת מתחברים לתמונה אחת.

--- PAGE ---

## מבנה התיקיות

מבנה כללי:

- `main.py` – נקודת כניסה
- `handlers/` – טיפול בהודעות ו־callbacks
- `callbacks/` – לוגיקת תפריטים וקורס
- `buttons/` – בניית תפריטים
- `utils/` – כלים כלליים (קונפיג, לוגים, טלגרם, i18n)
- `db/` – גישה למסד הנתונים
- `texts/` – טקסטים סטטיים דו־לשוניים
- `course/` – קבצי הקורס (Markdown)
- `landing/` – דף נחיתה

--- PAGE ---

## main.py – הלב של ה-API

- יוצר FastAPI
- מגדיר `/` ו־`/webhook`
- לא מכיל לוגיקה עסקית – רק ניתוב

זה הופך את הקוד לנקי וקל להבנה.

--- PAGE ---

## handlers/ – שכבת ניתוב לוגית

- `router.py` – הודעות טקסט
- `callback_router.py` – כפתורי Inline
- `admin.py` – לוגיקת אדמין
- `slots.py` – משחק SLOTS

כל handler אחראי על תחום אחד ברור.

--- PAGE ---

## callbacks/ – לוגיקת תפריטים וקורס

- `menu.py` – תפריט ראשי, תשלום, משחק, תמיכה
- `course.py` – הצגת שיעורים, מצב דמו, התקדמות

כאן מתבצעת הלוגיקה של "מה קורה כשמשתמש לוחץ על כפתור".

--- PAGE ---

## db/ – שכבת גישה לנתונים

- `connection.py` – חיבור ל-Postgres
- `admins.py` – טבלת אדמינים
- `buyers.py` – טבלת רוכשים
- `events.py` – לוג אירועים
- `slots.py` – היסטוריית משחק
- `course_progress.py` – התקדמות בקורס (Redis)
- `stats.py` – סטטיסטיקות לאדמין

המטרה: שכל גישה ל-DB תהיה מרוכזת במקום אחד.

--- PAGE ---

## utils/ – כלים משותפים

- `config.py` – קריאת משתני סביבה
- `telegram.py` – שליחת הודעות/קבצים
- `i18n.py` – ניהול שפות
- `edu_log.py` – לוגים חינוכיים
- `content.py` – קריאת קבצי Markdown

זה מה שמאפשר לקוד להיות DRY (לא לחזור על עצמו).

--- PAGE ---

## course/ – שכבת התוכן

- `course/he/*.md` – קורס בעברית
- `course/en/*.md` – קורס באנגלית
- כל קובץ מחולק לעמודים עם `--- PAGE ---`

הבוט לא "יודע" מה כתוב שם – הוא רק מציג את זה.

--- PAGE ---

## landing/ – דף נחיתה

- `index.html` – מבנה הדף
- `style.css` – עיצוב

אפשר להעלות את זה ל־Netlify / Vercel / GitHub Pages, או לשרת אחר.

--- PAGE ---

## סיכום

הארכיטקטורה בנויה כך שתוכל:

- להבין כל שכבה בנפרד
- להחליף חלקים (למשל: DB, דף נחיתה)
- להרחיב את המערכת (עוד משחקים, עוד קורסים, עוד תשלומים)

זה לא רק בוט – זו תבנית למוצרים דיגיטליים עתידיים.
